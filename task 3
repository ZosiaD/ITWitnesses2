'''
Generate a random number between 1 and 10. Ask the user to guess the number
and print a message based on whether they get it right or not.
'''

import random
number = int(input('Enter number between 1 and 10: '))
number_n = random.randint(1, 10)
while number != number_n:
    number = int(input('Try again) Enter number between 1 and 10: '))
print('You are right!')
